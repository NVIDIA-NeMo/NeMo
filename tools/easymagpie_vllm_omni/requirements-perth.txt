# Optional codec-output watermarking. resemble-perth 1.0.1 does not declare
# Requires-Dist, so list the packages the watermark path actually imports.
# Do not pin librosa/torchaudio transitives; let pip resolve them.
resemble-perth==1.0.1
librosa>=0.11.0
torchaudio
